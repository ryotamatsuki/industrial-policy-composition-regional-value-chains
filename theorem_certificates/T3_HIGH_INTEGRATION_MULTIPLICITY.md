# T3 — High-Integration Multiplicity

- Freeze: `IPCRVC-THEORY-FREEZE-2026-09-07-v1`
- Exact quantifiers: for all `Delta > 0`, `0 < alpha < 1`, and `A > Delta/(1-alpha)`.
- Exact claim: the exact pure-strategy Nash set is `{(1,0),(0,1),(q,q)}`, where `q = alpha + Delta/A` lies in `(0,1)`.
- Welfare claim: the differentiated equilibria are planner-optimal; the symmetric `(q,q)` equilibrium is welfare-inferior.
- Proof maturity: `PROVED`.
- Stage 4A: `PASS`.
- Verification: symbolic welfare-gap identity and regression R3.
- Prohibited wording: high integration makes every equilibrium efficient.
